# JazzFlow-RT Requirements

# Core
torch>=2.0.0
numpy>=1.24.0

# MIDI Processing
pretty-midi>=0.2.10
mido>=1.3.0

# Data Processing
tqdm>=4.65.0

# Training (Optional - for fine-tuning)
peft>=0.5.0
transformers>=4.30.0
bitsandbytes>=0.41.0
accelerate>=0.20.0

# Logging (Optional)
wandb>=0.15.0

# Audio (Optional - for live playback)
sounddevice>=0.4.6
soundfile>=0.12.1

# Development
pytest>=7.4.0
